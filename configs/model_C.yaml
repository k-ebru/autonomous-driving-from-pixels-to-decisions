# Model C: larger PPO clipping range.
# Allows stronger policy updates per epoch. Tests the trade off between
# faster early learning and optimisation stability.

run_name: model_C

env:
  env_id: CarRacing-v3
  n_envs: 4
  n_stack: 6
  skip_zoom_steps: 50
  frame_size: 84

seed: 42
eval_seed: 999

ppo:
  policy: CnnPolicy
  learning_rate: 0.0003
  n_steps: 1024
  batch_size: 256
  n_epochs: 4
  ent_coef: 0.002
  clip_range: 0.25
  max_grad_norm: 0.5

training:
  total_timesteps: 10000000
  time_limit_secs: 3600
  eval_freq: 25000
  n_eval_episodes: 3

evaluation:
  stochastic_episodes: 10
  deterministic_episodes: 5
  unseen_seeds: [101, 202, 303, 404, 505, 606, 707, 808, 909, 1000]
  solved_threshold: 900
